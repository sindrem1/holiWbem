import React from "react";

function Underhead(props) {
    return (
        <div className="heading">
            <h3 className="underhead">{props.content}</h3>
        </div>
    );
}
export default Underhead;